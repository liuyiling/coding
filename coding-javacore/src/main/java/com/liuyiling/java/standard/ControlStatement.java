package com.liuyiling.java.standard;

/**
 * Created by liuyiling on 17/5/24.
 */
public class ControlStatement {

    public static void main(String[] args) {
        //1.switch需要写全所有的可能
        int tempInt = 1;
        switch (tempInt) {
            case 1:
                //...
                break;
            case 2:
                //...
                break;
            default:
        }

        //2.if语句一定要挎号
        if (1 > 0) {
        }

        //3.使用多if语句替换if-else-if-else语句
        //4.不要在if条件中使用复杂的语句
        //5.try-catch中的块应该尽量少
        //6.

    }
}
